<p>
  <% if comment.user %>
    <small>
      submitted <%= time_ago_in_words(comment.created_at) %> ago by
      <em><%= comment.user.name || comment.user.email %></em>
    </small>
  <br>
  <%= comment.body %>
  <% end %>
</p>